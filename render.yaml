# ============================================================
# render.yaml - Infrastructure as Code pour Render
# Déploiement automatique du backend Flask + Frontend React
# ============================================================

services:
  # Backend Flask API (serves both API and frontend static files)
  - type: web
    name: douanesci-convocation
    runtime: docker
    dockerfilePath: ./Dockerfile
    region: frankfurt
    plan: starter
    healthCheckPath: /health
    numInstances: 1
    
    # Variables d'environnement pour le backend
    envVars:
      - key: DB_HOST
        sync: false  # Configure in Render Dashboard
      - key: DB_PORT
        sync: false
      - key: DB_USER
        sync: false
      - key: DB_PASSWORD
        sync: false
      - key: DB_NAME
        sync: false
      - key: JWT_SECRET_KEY
        sync: false  # Auto-generated on first deployment
      - key: FLASK_ENV
        value: production
      - key: FLASK_DEBUG
        value: "0"
      - key: PORT
        value: "5000"
